---
/**
 * PostCard — preview tile for one post in the /blog list; the whole card is
 * the link.
 *
 * Props:
 *   - post: a blog collection entry (frontmatter in `post.data`)
 */
import type { CollectionEntry } from 'astro:content';

interface Props {
  post: CollectionEntry<'blog'>;
}

const { post } = Astro.props;
const { title, description, date, tags } = post.data;

// timeZone: 'UTC' or the date shows a day early — a bare YAML date parses as
// UTC midnight, and we're west of UTC.
const formattedDate = date.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
  timeZone: 'UTC',
});
---

<a
  href={`/blog/${post.id}`}
  class="block rounded-lg border border-line p-5 transition-colors hover:border-accent"
>
  <p class="font-mono text-xs text-muted">
    <time datetime={date.toISOString()}>{formattedDate}</time>
    {tags.length > 0 && <span> · {tags.join(', ')}</span>}
  </p>
  <h2 class="mt-1 font-heading text-xl font-bold text-text">{title}</h2>
  <p class="mt-2 leading-relaxed text-muted">{description}</p>
</a>
